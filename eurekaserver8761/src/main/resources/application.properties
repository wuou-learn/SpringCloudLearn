# 服务中心端口号
server.port=8761
spring.application.name=eurekaServer
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# 告诉客户端启动不立即注册
eureka.client.fetch-registry=false

# 关闭自己注册自己，仅仅当做server端
eureka.client.register-with-eureka=false